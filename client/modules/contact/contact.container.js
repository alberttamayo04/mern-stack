import { connect } from 'react-redux';
import contactPresentational from './contact';

const mapStateToProps = () => ({

});

const mapDispatchToProps = () => ({
});

const contact = connect(mapStateToProps, mapDispatchToProps)(contactPresentational);

export default contact;
